{% extends 'base.html.twig' %}

{% block nav %}

<nav>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <ul class="nav nav-pills nav-fill">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('backend_user_show', {'id': app.user.id}) }}">Mon profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('backend_question_new') }}">Poser une question</a>
                </li>
        </ul>
    {% endif %}
</nav>


{% endblock %}
